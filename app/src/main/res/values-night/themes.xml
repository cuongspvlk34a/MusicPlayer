<?xml version="1.0" encoding="utf-8"?>
<resources>

    <!-- Theme chính (dark, không ActionBar) -->
    <style name="Theme.MusicPlayer" parent="Theme.MaterialComponents.DayNight.NoActionBar">
        <item name="colorPrimary">@color/colorPrimary</item>
        <item name="colorPrimaryVariant">@color/colorPrimaryDark</item>
        <item name="colorOnPrimary">@color/white</item>
        <item name="android:windowBackground">@color/colorBackground</item>
        <item name="android:statusBarColor">@color/colorBackground</item>
        <item name="android:navigationBarColor">@color/colorBackground</item>
    </style>

    <!-- SplashActivity: fullscreen qua theme, không dùng FLAG_FULLSCREEN trong code -->
    <style name="Theme.MusicPlayer.Splash" parent="Theme.MusicPlayer">
        <item name="android:windowFullscreen">true</item>
        <item name="android:windowNoTitle">true</item>
    </style>

    <!-- PlayerActivity: status bar trong suốt để gradient chạm đến trên -->
    <style name="Theme.MusicPlayer.Player" parent="Theme.MusicPlayer">
        <item name="android:statusBarColor">@android:color/transparent</item>
        <item name="android:windowTranslucentStatus">true</item>
    </style>

</resources>